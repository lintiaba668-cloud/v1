# AGENTS.md

## 项目定位

PowerRename V1 是面向配网工程资料的 Windows 桌面批处理工具。用户先导入工程明细
Excel，再导入手机拍摄的开工报告或竣工验收报告图片。程序通过内置 Tesseract OCR
提取工程名称和工程编号，与工程明细库匹配后，把原图复制到输出目录并按标准规则命名。

本项目的目标运行环境是：

- Windows 7 32 位和 64 位；
- 绿色免安装目录；
- Python 3.8 构建的 PyInstaller `onedir` 程序；
- 离线运行，不依赖网络服务；
- 内置 Tesseract、中文和英文语言包。

不要把“当前开发机可以运行”等同于“Win7 绿色包已经完成”。只有分别使用 Python 3.8
x86/x64、匹配架构的 OCR 引擎完成打包和目标机冒烟测试后，才能宣称对应版本可发布。

## 不可破坏的业务规则

1. 开工报告输出名必须为：
   `工程名称_工程编号_开工.<原扩展名>`
2. 竣工验收报告输出名必须为：
   `工程名称_工程编号.<原扩展名>`
3. 最终工程名称和工程编号必须来自用户导入的工程明细，不得直接把未经匹配的 OCR
   文本当成标准文件名。
4. 开工报告自身没有可靠的工程编号字段。日期、杆号、检查项数字等不得作为工程编号；
   应使用识别出的工程名称匹配工程明细后取得编号。
5. 当工程名称重名、照片编号与 Excel 冲突、候选分数过近或信息不足时，必须进入
   `review_required` / `unmatched`，不得为了提高“成功率”而自动选择不确定项目。
6. 输出采用复制，必须保留用户原始图片。禁止移动、覆盖或删除输入文件。
7. 重名输出使用 `_2`、`_3` 等后缀，不能覆盖已经生成的文件。
8. Windows 文件名非法字符必须清理，原图片扩展名应保留并转为小写。

实际命名逻辑以 `core/ocr_rename_service.py` 和 `core/rename.py` 为准。README 中若有旧规则，
应同步修正文档，而不是让代码退回旧行为。

## 当前生产入口与调用链

当前 GUI 入口是 `main.py`，主要调用链如下：

```text
main.py
  -> ui/main_window_v3.py
  -> core/final_runner.py
  -> core/batch_process.py
  -> core/ocr_rename_service.py
  -> ocr/pipeline_ocr.py
  -> ocr/ocr_engine.py
  -> ocr/template_field_recognizer.py
  -> project/project_service.py
  -> project/match_manager.py
  -> core/rename.py
```

工程明细导入链路：

```text
ui/import_dialog.py
  -> ui/excel_loader.py
  -> project/project_service.py
  -> project/import_manager.py
  -> data/projects.db
```

批处理完成后，`core/excel_result_writer.py` 在输出目录生成 `重命名记录.xlsx`。

`core/batch_process.py` 采用两阶段识别。第一阶段先对全部图片运行 `fast` 模式，按逻辑
CPU 数选择图片级 OCR 工作线程：1～3 个逻辑 CPU 使用 1 个，4 个及以上使用 2 个，当前
最大并发数为 2。快速阶段只有 `success` 才会输出；其他结果进入困难队列。第二阶段使用
`deep` 模式按原导入顺序单线程重试困难图片，并在深度结果更差时保留快速阶段较好的人工
复核候选。每个快速线程必须持有独立的 OCR 引擎/执行器；工程库使用线程安全的只读内存
索引共享，并在工程明细导入成功后失效重载。最终文件名分配和复制必须通过同一把锁串行
执行，避免同名目标并发覆盖。低内存环境可设置 `POWER_RENAME_OCR_WORKERS=1` 关闭快速
阶段并行。

批处理仅在图片取得最终阶段结果时发送普通进度回调，同时用独立阶段回调报告快速扫描
进度、困难队列和精细重试剩余量；返回结果必须继续保持用户导入顺序。主界面据此更新
百分比、阶段文字并标亮已完成图片。

仓库中存在 `ocr/lite_parser_*`、`ocr/parser_v2.py`、`ocr/pipeline.py`、
`batch/zip_processor_v24.py`、`build/` 等历史或兼容模块。修改前必须从上述生产调用链确认
模块是否真正被使用，不要只因文件名相似就改错入口。若要删除历史代码，必须先证明没有
运行时导入、测试依赖或打包 hidden import。

## 输入、输出和持久化数据

- 支持图片：`.jpg`、`.jpeg`、`.png`、`.bmp`、`.tif`、`.tiff`。
- GUI 支持单张/多张图片、文件夹和 ZIP。
- ZIP 解压必须防止路径穿越；生产实现位于 `ui/main_window_v3.py`。
- 导入首批图片或文件夹后，默认输出目录为该输入路径同级的 `输出文件夹/`；用户手动
  选择输出目录后不得再被后续导入自动覆盖。
- 主界面的“打开输出目录”位于“选择输出目录”下方，仅在当前输出目录已经存在且未在
  批处理中时启用，并通过 Windows 资源管理器打开该目录。
- 日志位于 `logs/PowerRename.log`。
- OCR 调试裁切图位于 `logs/template_ocr/`。
- ZIP 临时解压目录位于 `temp/imported/`。
- 工程库位于 `data/projects.db`，这是用户运行数据，不是测试夹具。

不要把用户图片、工程明细、生成结果、日志、临时目录或修改后的 `projects.db` 当作源代码
改动提交或覆盖。测试数据库必须使用 pytest 的临时目录或显式的临时路径。

主界面的“缓存清理”只允许清空 `logs/template_ocr/` 和 `temp/imported/` 内的软件生成
文件，不得删除 `PowerRename.log`、工程库或输出目录。如果待处理列表仍引用 ZIP 临时
图片，必须跳过整个 `temp/imported/`，避免使当前任务中的路径失效。

工程明细 Excel 的约定：

- 仅支持 `.xlsx`；
- 第一行为表头；
- 第一列是工程编号；
- 第二列是工程名称；
- 编号可能包含字母、数字、`-` 或 `#`，也可能由 Excel 以数值形式返回；
- 同名工程可能对应多个编号，因此仅凭名称不一定能自动确认。

## OCR 实现约束

### 当前策略

`ocr/template_field_recognizer.py` 是首选识别器：

1. 应用 EXIF 方向；
2. 对宽度超过 2200 像素的手机原图建立等比例 OCR 工作副本，输出复制仍使用未改写的原图；
3. 尝试 0/90/180/270 度，依据标题和表格结构选择方向；
4. 仅对明显的小角度倾斜做 Pillow 纠偏；
5. 竣工报告优先定位“工程名称”和“工程编号”表格区域；
6. 开工报告只裁切标题下方到“开工，请审批”一栏（当前下边界为整图高度 20%），
   提取“我方完成”附近的工程名称，避免把检查清单、印章和日期送入字段 OCR；
7. 字段 OCR 先跑灰度快速通道；取得可靠的标准编号或高质量名称后停止，其余情况继续二值化和不同 PSM 的完整重试；
8. 竣工编号常规裁图失败时，按需重试单元格内侧、上半区、更窄的顶部区域以及两个常见首行位置；异常宽的“工程编号”标签框必须退回保守单元格边界；
9. `fast` 模式只运行常规裁图、灰度单 PSM 字段 OCR，不进入高分辨率、探测框或 legacy fallback；
10. `deep` 模式只用于困难队列，可使用 3200/3600 像素工作宽度、二值化、多 PSM、开工报告两行长工程名称专用中文裁切、额外探测框和原图顶部 30% 的竣工字段兜底；
11. 保留多个名称/编号候选，交由 Excel 匹配层选择；
12. `deep` 模式模板字段仍不可靠时才进入 legacy fallback。

Tesseract TSV 是制表符分隔文本，不使用 CSV 引号语义。解析 TSV 时必须使用
`csv.QUOTE_NONE`，否则 OCR 输出的单个双引号会吞并后续行并破坏字段坐标。

### 兼容要求

- `opencv-python` 不是 Win7 发布依赖。OpenCV 透视校正只能作为可选能力，必须保留
  Pillow-only 的可用路径。
- 不要增加云 OCR、在线模型或运行时下载。
- Tesseract 由 `ocr/ocr_executor.py` 通过子进程调用，资源由 `core/resource.py` 定位。
- 打包目录必须包含 `engine/tesseract.exe`、`engine/tessdata/chi_sim.traineddata` 和
  `engine/tessdata/eng.traineddata`。
- OCR 对手机照片的纸张纹理、表格线、印章和轻微倾斜敏感。修改裁切比例时必须查看
  `logs/template_ocr` 中的实际裁切图，不能只凭单元测试判断。
- 小角度旋转会引入插值模糊；除非真实样张证明有收益，不要对约 1° 的图片强制旋转。
- 工程编号候选可以去除明显的边框噪声前缀，但不得凭空改写为某个 Excel 编号。

## 工程匹配安全边界

`project/match_manager.py` 当前按以下优先级工作：

1. 工程编号精确匹配；
2. 12 位字母数字主码的业务序号等价匹配（`-N` 等价于两位尾码 `N+1`，例如
   `-1 ↔ 02`、`-2 ↔ 03`、`-5 ↔ 06`、`015D-15 ↔ 015D16`）；
3. 唯一的常见字形混淆编号匹配；
4. 唯一的缺少少量前缀/字形混淆编号匹配；
5. 唯一的一编辑距离编号匹配；
6. 工程名称评分匹配；
7. 分数或候选间距不足时人工复核。

名称评分中的长线路允许少量 OCR 错字或分段粘连，变电站名称允许稳定后缀一致且前缀少量错字；真正不同的线路、杆号仍必须保留冲突扣分。
编号字形混淆当前覆盖 `L/I/1`、`D/O/Q/0`、`C/0` 和 `Z/2`，且只能在工程库中唯一对应一个编号时自动接受。
表格线裁掉固定编号前缀时，可以生成 `181320`、`C513`、`B113` 格式的保守候选；
所有候选仍须精确命中工程库。若不同候选分别命中不同工程，必须进入人工复核。

重新导入程序已经生成的规范文件名时，仅当“文件名工程名称”和“文件名工程编号”同时
与工程库同一条记录严格一致，才允许把它作为 OCR 失败后的兜底；名称或编号任一不符均
不得信任文件名。

业务序号换算只允许用于“12 位字母数字主码 + 序号”的格式；主码长度限制用于防止
`B1132025Z497` 等普通工程编号被误拆为子项目序号。

保留“唯一性”和“最小分差”保护。不要仅因为某个候选排第一就自动接受。尤其注意：

- 报告上印刷的编号可能与最新 Excel 不一致；
- Excel 可能包含同名、不同编号的工程；
- 开工报告没有编号时，同名工程必须由用户确认；
- 错误重命名比进入人工复核更严重。

任何调整阈值、归一化或模糊匹配的改动，都要增加冲突用例和反例测试。

## 本地开发与验证

在项目根目录使用 PowerShell：

```powershell
python main.py
python -m pytest -q
python build_check.py
python ocr_diagnostic.py "C:\path\to\images"
```

当前自动化测试基线为 163 项通过。新增功能或修复必须添加针对性测试，并确保完整测试集通过。
不要只运行被修改模块的测试后就交付。

真实图片回归至少检查：

- 开工/竣工类型是否正确；
- 0/90/180/270 度方向是否正确；
- 倾斜图片校正后文字是否更清晰；
- 名称和编号裁切是否完整；
- 首位数字、末位数字、连字符是否保留；
- 开工报告编号是否保持为空直到 Excel 匹配；
- 标准 Excel 编号能否精确匹配；
- 冲突和重名是否进入人工确认；
- 输出文件名和 `重命名记录.xlsx` 是否一致；
- 输入原图是否仍然存在且未被改写。

诊断工具会生成日志和裁切图。诊断产物可以用于分析，但不应成为单元测试对固定绝对路径的
依赖。测试中不要硬编码开发机的 `G:\`、Downloads 或桌面路径。

## Win7 打包

发布入口是根目录的 `build_win7.bat` 和 `build_win7.spec`。

- 必须使用 Python 3.8；
- x86 和 x64 要在对应位数的 Python 环境分别构建；
- 使用 `requirements-win7.txt` 中固定版本；
- PyInstaller 模式为 `onedir`，不是单文件；
- 构建前运行完整测试；
- 构建后确认 `release/PowerRename_Win7_x86` 或
  `release/PowerRename_Win7_x64` 中存在 `PowerRename.exe`；
- 确认 OCR 引擎及 DLL 架构与目标包一致；
- 在无 Python 的目标 Windows 机器上做启动、Excel 导入、真实图片识别和输出测试。

如果当前机器只有 Python 3.11，允许做源码开发和测试，但不能据此宣布 Win7 发布包已完成。
不要让 PyInstaller 清理命令误删项目根目录、用户数据或不相关构建产物。

## 编码与实现风格

- 源码保持 UTF-8，中文业务词和错误信息应可读，避免引入乱码。
- 所有生产代码保持 Python 3.8 语法兼容；不要使用 `match`、`X | Y` 类型联合等新语法。
- 路径处理优先使用 `pathlib.Path`，同时验证 Windows 非 ASCII 路径。
- 新增依赖前先评估 Win7、x86、离线打包和体积影响；能用标准库/Pillow 完成时不要增加重依赖。
- UI 线程中不要执行长时间 OCR；继续通过 `RunnerThread` 运行批处理。
- 批处理单张失败不应中断整批，应返回可审计的状态和错误。
- 安全相关逻辑（ZIP 路径检查、文件名清理、输入保留、人工复核）必须有回归测试。

## 修改流程与完成标准

1. 先确认修改点是否位于当前生产调用链。
2. 对 bug 先保留最小复现或真实诊断证据。
3. 在临时目录/测试数据库中实现和验证，避免污染用户工程库与输出。
4. 添加单元测试；OCR 裁切或匹配变化还要运行代表性真实样张。
5. 运行完整 `python -m pytest -q`。
6. 回写或交付后核对目标文件内容/哈希，避免只修改了副本。
7. 明确区分“源码已更新”“开发机测试通过”“Win7 x86/x64 绿色包已构建并验证”三种状态。

满足以下条件才算完成：

- 业务命名规则没有退化；
- 输入原图未被破坏；
- 自动匹配只接受可靠且唯一的结果；
- 不确定结果可以人工处理；
- 完整自动化测试通过；
- 真实样张回归结果有记录；
- 若交付 EXE，对应架构的绿色包已实际构建并在目标环境验证。
